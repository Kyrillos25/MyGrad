# SQL Server Configuration
# Set these environment variables to configure SQL Server authentication

# SQL Server username (default: sa)
SQL_USERNAME=sa

# SQL Server password (change this to your actual password)
SQL_PASSWORD=your_password_here

# SQL Server name/IP (default: localhost)
SQL_SERVER=localhost

# SQL Server database name (default: TestDB)
SQL_DATABASE=TestDB